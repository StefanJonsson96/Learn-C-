#include "all_includes.h"

int main() {
   // pointers();
   // puts(""); // Print a newline
   // sizeOfTypes();
   // tempConversion();
   // strings();
   // readInput();
   // stringConcatenation();
   // stringConcatenationSafe();
   // ifElse();
   // switchCase();
   // ternaryOperator();
   forLoop();
   whileLoop();
   doWhileLoop();
   
   return 0;
}
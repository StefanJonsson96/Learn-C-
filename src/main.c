#include <stdio.h>
#include "pointers.h"
#include "sizeOfTypes.h"
#include "tempConversion.h"

int main() {
   // pointers();
   // puts(""); // Print a newline
   // sizeOfTypes();
   // tempConversion();
   // strings();
   // readInput();
   stringConcatenation();
   stringConcatenationSafe();
   return 0;
}